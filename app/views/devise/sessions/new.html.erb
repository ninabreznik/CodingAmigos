

<div class="body_devise_signup">
<div class="signup">
  <br>
  <div class="signup_title">
    <%= I18n.t'users-login.login' %>
  </div>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="signup_form_email"><%= f.label :"#{I18n.t'users-login.email'}" %><br />
  <%= f.email_field :email, autofocus: true %></div>

  <div class="signup_form_password"><%= f.label :"#{I18n.t'users-login.password'}" %><br />
    <%= f.password_field :password, autocomplete: "off" %></div>

  <% if devise_mapping.rememberable? -%>
    <div class="signup_form_password"><%= f.check_box :remember_me %> <%= f.label :"#{I18n.t'users-login.remember-me'}" %></div>
  <% end -%>

  <div class="signup_form_submit"><%= f.submit "#{I18n.t'users-login.login'}" %></div>
<% end %>
<div class="signup_shared_links"><br><br>
  <%= render "devise/shared/links" %>
</div>
</div>

</div>
