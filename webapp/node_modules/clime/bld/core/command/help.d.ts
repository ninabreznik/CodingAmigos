import { Command } from './';
import { Printable } from '../object';
export declare class HelpInfo implements Printable {
    private texts;
    constructor();
    text: string;
    static build(dir: string, description?: string): HelpInfo;
    static build(CommandClass: typeof Command): HelpInfo;
    private buildDescription(description);
    private buildTextsForParamsAndOptions(CommandClass);
    buildTextForSubCommands(dir: string): void;
    print(stdout: NodeJS.WritableStream, stderr: NodeJS.WritableStream): void;
}
