import { Validator } from '../core/';
export declare class RangeValidator implements Validator<number> {
    /** value >= from. */
    from: number;
    /** value < to. */
    to: number;
    constructor(
        /** value >= from. */
        from: number, 
        /** value < to. */
        to: number);
    validate(value: number, name: string): void;
}
export declare function range(from: number, to: number): RangeValidator;
export declare class IntegerValidator implements Validator<number> {
    validate(value: number, name: string): void;
}
export declare const integer: IntegerValidator;
